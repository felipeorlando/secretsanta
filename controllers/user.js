class User {
  index(req, res) {
    res.status(200).send('It work!');
  }

  create(req, res) {
    res.status(200).send('It work!');
  }

  show(req, res) {
    res.status(200).send('It work!');
  }

  update(req, res) {
    res.status(200).send('It work!');
  }

  delete(req, res) {
    res.status(200).send('It work!');
  }
}

export default User;
